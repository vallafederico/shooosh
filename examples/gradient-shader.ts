// Generated by shooosh. Do not edit.
export const fragment = "fn fsMain() -> vec4f {\n  let t = uUni.values0.x;\n  let ink = vec3f(0.047, 0.047, 0.043);\n  let acid = vec3f(0.847, 1.0, 0.243);\n  let paper = vec3f(0.925, 0.906, 0.863);\n  let g = mix(ink, paper, vUv.y);\n  let band = smoothstep(0.46, 0.5, vUv.x) * (1.0 - smoothstep(0.5, 0.54, vUv.x));\n  let pulse = 0.65 + 0.35 * sin(t * 2.0);\n  return vec4f(mix(g, acid, band * pulse), 1.0);\n}\n";
export const fragmentGlsl = "#version 300 es\nprecision highp float;\nin vec2 vUv;\nuniform vec4 uUni[4];\nout vec4 outColor;\nvoid main() {\n  float t = uUni[0].x;\n  vec3 ink = vec3(0.047, 0.047, 0.043);\n  vec3 acid = vec3(0.847, 1.0, 0.243);\n  vec3 paper = vec3(0.925, 0.906, 0.863);\n  vec3 g = mix(ink, paper, vUv.y);\n  float band = smoothstep(0.46, 0.5, vUv.x) * (1.0 - smoothstep(0.5, 0.54, vUv.x));\n  float pulse = 0.65 + 0.35 * sin(t * 2.0);\n  outColor = vec4(mix(g, acid, band * pulse), 1.0);\n  return;\n\n}\n";
export default { fragment, fragmentGlsl };
